machine:
  timezone:
    Asia/Tokyo

checkout:
  override:
    - git clone https://github.com/corgisamurai/spary_go.git 

dependencies:
  override:
    - go get -h

test:
  override:
    - cd spary_go 
    - go test -bench . -benchmem
