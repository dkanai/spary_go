machine:
  timezone:
    Asia/Tokyo

checkout:
  override:
    - git clone https://github.com/corgisamurai/spary_go.git 

dependencies:
  override:
    - glide update

test:
  override:
    - cd spary_go 
    - go test -bench . -benchmem
