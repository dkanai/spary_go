machine:
  timezone:
    Asia/Tokyo

dependencies:
  override:
    - glide install

checkout:
  override:
    - git clone https://github.com/corgisamurai/spary_go.git 

dependencies:
  override:
    - glide update
    
test:
  override:
    - cd spary_go
    - sh ./test.sh
