machine:
  timezone:
    Asia/Tokyo

dependencies:
  override:
    - go get -h

checkout:
  override:
    - git clone https://github.com/corgisamurai/spary_go.git

test:
  override:
    - cd spary_go
    - sh ./test.sh
